{
  "name": "product-catalog-service",
  "version": "1.0.0",
  "description": "Product catalog service with Elasticsearch search",
  "main": "index.js",
  "scripts": {
    "start": "npx prisma migrate deploy && node index.js",
    "dev": "nodemon index.js",
    "db:migrate": "prisma migrate dev",
    "db:generate": "prisma generate",
    "db:reset": "prisma migrate reset",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "product",
    "catalog",
    "elasticsearch",
    "prisma"
  ],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "express": "^4.18.2",
    "@prisma/client": "^5.0.0",
    "@elastic/elasticsearch": "^8.10.0"
  },
  "devDependencies": {
    "prisma": "^5.0.0",
    "nodemon": "^3.0.0"
  }
}
